pipeline {
   agent any
   stages {
       stage('Github Push') {
           steps {
            
              checkout scm 
           }
       }
      stage('Docker Build') {
           steps {
           
           }
       }
   }
}
